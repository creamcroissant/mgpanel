services:
  mgpanel:
    image: mgpanel:latest
    build:
      context: .
      args:
        - VERSION=${VERSION:-dev}
        - COMMIT=${COMMIT:-unknown}
        - BUILD_TIME=${BUILD_TIME:-unknown}
    container_name: mgpanel
    restart: unless-stopped
    volumes:
      # SQLite database persistence
      - mgpanel-data:/data
      # Configuration file (create config.yml first)
      - ./config.yml:/etc/mgpanel/config.yml:ro
    environment:
      - MGPANEL_DB_PATH=/data/mgpanel.db
      - MGPANEL_HTTP_ADDR=:8080
      - TZ=Asia/Shanghai
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "50m"
        max-file: "5"

volumes:
  mgpanel-data:
    driver: local
