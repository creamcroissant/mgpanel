version: '3.8'

services:
  xboard:
    build: .
    environment:
      - GO_ENV=development
      - XBOARD_LOG_LEVEL=debug
    volumes:
      - ./data:/data
      # Optional: mount source code if using a dev container with air/reflex
      # - .:/app